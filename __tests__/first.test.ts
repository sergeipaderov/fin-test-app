// The app uses "react": "19.0.0".
// @testing-library/react-native replaces the deprecated
// react-test-renderer because react-test-renderer does not support
// React 19 and above.
// https://docs.expo.dev/develop/unit-testing

// I integrated Jest into the app (according with bonus requirement) and it works with "npm run test" command.
// But version conflict needs to be resolved to add more tests.

it("works", () => {
  expect(1).toBe(1);
});
